name: 04- Using Actions

on: push

jobs:
    Build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout file
              uses: actions/checkout@v4
            - name: Printing folders
              run: ls

